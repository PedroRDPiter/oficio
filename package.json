{
  "name": "control-oficios-dpdu",
  "version": "1.0.0",
  "private": true,
  "description": "PWA para recepcion, asignacion y consecutivos de oficios.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check sw.js"
  },
  "engines": {
    "node": ">=18"
  }
}
